# Local OMR backend. One service — nothing else needed.
services:
  api:
    build: .
    ports:
      - "8000:8000"
    # Live-reload the app code during development.
    volumes:
      - ./app:/app/app
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000", "--reload"]
